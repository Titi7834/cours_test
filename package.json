{
  "dependencies": {
    "jest": "^30.2.0"
  }
  ,"scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  }
  ,"jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "script.js"
    ],
    "coverageDirectory": "coverage",
    "coverageReporters": [
      "text",
      "lcov"
    ]
  }
}
